// prisma/schema.prisma

// --- Generator: Prisma Client ---
generator client {
  provider = "prisma-client-js"
}

// --- Datasource: choose your DB ---
// Dev (SQLite). For prod, switch provider to "postgresql" and update DATABASE_URL.
datasource db {
  provider = "postgresql"
  url      = env("postgresql://neondb_owner:npg_Bkqua0ONmTJ9@ep-late-math-ad4og07y-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require")
}

// --- Models ---

model Lead {
  id        String   @id @default(cuid())
  name      String?
  email     String
  phone     String
  message   String?
  source    String?
  createdAt DateTime @default(now())
}

model Order {
  id              String      @id @default(cuid())
  razorpayOrderId String?
  status          OrderStatus @default(PENDING)
  amount          Int // paise
  currency        String      @default("INR")
  receipt         String?
  paymentId       String?
  signature       String?
  createdAt       DateTime    @default(now())
  items           OrderItem[]
}

model OrderItem {
  id        String @id @default(cuid())
  order     Order  @relation(fields: [orderId], references: [id], onDelete: Cascade)
  orderId   String
  productId String
  name      String
  size      String
  price     Int // per-unit price in paise
  qty       Int
}

enum OrderStatus {
  PENDING
  PAID
  FAILED
}
